// nummber, string, boolean, undefined, bigInt, Symbol (stack)
// Non-Primtive: Array, Object, Function (Heap)

let num = 15;
console.log(typeof num);

let name = String(num);
console.log(`${typeof name} && ${name}`);

